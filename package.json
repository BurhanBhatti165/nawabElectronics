{
  "name": "nawab",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "preview:frontend": "npm run preview --prefix frontend",
    "dev:backend": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir backend",
    "start:backend": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir backend"
  }
}
